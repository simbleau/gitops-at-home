# /etc/exports: the access control list for filesystems which may be exported
#               to NFS clients.  See exports(5).
#
{% for client in groups['client'] %}
{{ hostvars[inventory_hostname].mnt }}     {{ hostvars[client].ansible_host }}/24(rw,insecure,no_subtree_check,all_squash,anonuid=0,anongid=0)
{% endfor %}
