# /etc/exports: the access control list for filesystems which may be exported
#               to NFS clients.  See exports(5).
#
{% for client in groups['client'] %}
{{ hostvars[inventory_hostname].mnt }}     {{ hostvars[client].ansible_host }}/24(rw,sync,no_subtree_check,no_root_squash)
{% endfor %}

# Manual entries
{{ hostvars[inventory_hostname].mnt }}     spencer-mac/24(rw,sync,no_subtree_check,no_root_squash)
